apiVersion: v1
kind: ConfigMap
metadata:
  name: prometheus-config
  namespace: pharmago
data:
  prometheus.yml: |
    global:
      scrape_interval: 5s

    scrape_configs:
      - job_name: 'otlp-collector'
        metrics_path: /metrics
        static_configs:
          - targets: ['otlp-collector:8889']
      
      # Servicios multi-réplica: se scrapea CADA pod por separado (no el ClusterIP
      # del Service, que balancearía cada scrape a un pod distinto y mezclaría los
      # counters por-pod). El instance queda por pod y PromQL agrega con sum().
      - job_name: 'users-service'
        metrics_path: /metrics
        kubernetes_sd_configs:
          - role: endpoints
            namespaces:
              names: ['pharmago']
        relabel_configs:
          - source_labels: [__meta_kubernetes_service_name]
            regex: pharmago-users-service
            action: keep
          - source_labels: [__meta_kubernetes_endpoint_ready]
            regex: "true"
            action: keep
          - source_labels: [__meta_kubernetes_pod_name]
            target_label: instance

      - job_name: 'pharmacy-service'
        metrics_path: /metrics
        kubernetes_sd_configs:
          - role: endpoints
            namespaces:
              names: ['pharmago']
        relabel_configs:
          - source_labels: [__meta_kubernetes_service_name]
            regex: pharmago-pharmacy-service
            action: keep
          - source_labels: [__meta_kubernetes_endpoint_ready]
            regex: "true"
            action: keep
          - source_labels: [__meta_kubernetes_pod_name]
            target_label: instance

      - job_name: 'api-gateway'
        metrics_path: /metrics
        kubernetes_sd_configs:
          - role: endpoints
            namespaces:
              names: ['pharmago']
        relabel_configs:
          - source_labels: [__meta_kubernetes_service_name]
            regex: pharmago-api-gateway
            action: keep
          - source_labels: [__meta_kubernetes_endpoint_ready]
            regex: "true"
            action: keep
          - source_labels: [__meta_kubernetes_pod_name]
            target_label: instance

      - job_name: 'node-exporter'
        metrics_path: /metrics
        static_configs:
          - targets: ['node-exporter:9100']

      - job_name: 'kube-state-metrics'
        metrics_path: /metrics
        static_configs:
          - targets: ['kube-state-metrics:8080']

      # cAdvisor: métricas por contenedor/pod (para ver impacto de chaos)
      - job_name: 'kubernetes-cadvisor'
        kubernetes_sd_configs:
        - role: node
        scheme: https
        tls_config:
          ca_file: /var/run/secrets/kubernetes.io/serviceaccount/ca.crt
        bearer_token_file: /var/run/secrets/kubernetes.io/serviceaccount/token
        relabel_configs:
        - action: labelmap
          regex: __meta_kubernetes_node_label_(.+)
        - target_label: __address__
          replacement: kubernetes.default.svc:443
        - source_labels: [__meta_kubernetes_node_name]
          regex: (.+)
          target_label: __metrics_path__
          replacement: /api/v1/nodes/${1}/proxy/metrics/cadvisor
        - source_labels: [__meta_kubernetes_node_name]
          target_label: node
        - replacement: kubernetes-cadvisor
          target_label: job

